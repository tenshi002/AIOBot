modeles\Commande:
  type: entity
  table: commande
  repositoryClass: repositories\CommandeRepository
  fields:
    id:
      type: integer
    nom:
      unique: true
    sound:
      unique: true
  oneToMany:
    reponses:
      targetEntity: modeles\Texte
      mappedBy: nom
